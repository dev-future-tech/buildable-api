@Library('welcome-java') _

pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                echo 'Starting build...'
                javabuild
            }
        }
    }
}